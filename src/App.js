import React from 'react';
import { Routes, Route } from 'react-router-dom';

function App() {
  return (
    <>
    <h1>Title Goes Here</h1>

    {/* Insert Component elements, define paths and uncomment Routes block below to enabling routing */}
    
    {/* <Routes>
        <Route path='/' element={<EnterComponentHere />} />
        <Route path='page-1' element={<EnterComponentHere />} />

        //Wildcard for any 404s
        <Route path='*' element={<EnterComponentHere />} />
     </Routes> */}
    </>
  );
}

export default App;